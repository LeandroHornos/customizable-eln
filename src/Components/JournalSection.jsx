import React from "react";

const JournalSection = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <p> Esta es la sección de bitacora</p>
    </div>
  );
};

export default JournalSection;
