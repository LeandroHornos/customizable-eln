import React from "react";

const FormSection = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <p> Esta es la sección de formularios</p>
    </div>
  );
};

export default FormSection;
