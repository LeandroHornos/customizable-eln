import React from "react";

const TextSection = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <p> Esta es una sección de texto</p>
    </div>
  );
};

export default TextSection;
