import React from "react";

const TableSection = (props) => {
  return (
    <div>
      <h1>{props.name}</h1>
      <p> Esta es una sección de tabla</p>
    </div>
  );
};

export default TableSection;
