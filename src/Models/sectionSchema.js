const sectionSchema = {
  name: "", // El nombre que se muestra como título de la sección
  order: null, // Para indicar en que posicion mostrar la sección
  component: "", // Indica el componente a renderizar
  data: {}, // Los datos que se muestran dentro del componente
};

export default sectionSchema;
